# v0.1.18-desktop.1

- Treat blank or whitespace-only captain `claim_task.assignee` as omitted by
  reusing the existing optional-string normalizer; trim non-empty member names
  before lookup.
- Keep `assignee="captain"`, unknown members, and unauthorized claims as strict
  no-write failures. Captain takeover of member work still requires an explicit
  reassignment.
- Clarify create/reassign versus claim syntax inside the existing compact
  captain prompt without adding tools, prompt plugins, or gateway APIs.
- Do not submit Harness 0.1.5 scope-local `subagent` to global
  `tools.restrict()` or the default-depth child `toolFilter`; retain the Team
  execution guard and durable member-depth gateway instead.
- Retain upstream AgentTeams `v0.1.18` and its recommended Harness
  `0.1.5-rc.1` host unchanged.
