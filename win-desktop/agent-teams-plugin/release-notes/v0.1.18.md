# v0.1.18

- Adopt upstream lazy member activation: roster and task planning are durable before a member session is started.
- Add durable mailbox receipts, current-attempt filtering, obsolete-message discard, and settlement de-duplication.
- Route next-step member delivery through the existing durable AgentTeams gateway, preserving retirement and child-lock checks.
- Default member delegation depth to zero and reject stopped members at step admission.
- Retain local role route/reasoning policy, strict V2, quality, Profiles, Team/Native, authenticated Web, and durable-gateway invariants.
