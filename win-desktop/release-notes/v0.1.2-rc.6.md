# DeepSeek Harness Windows v0.1.2-rc.6

## 子智能体统一网关并发收口

- AgentTeams 的删除、任务重分配、审批失败和启动异常清理都通过统一 durable Session 子智能体网关及每子 Agent 锁执行退休、打断和静默等待。
- 删除与发送的真实交错回归通过：已经进入网关的投递会先完成，Team 才归档；后续冷恢复仍被退休索引拒绝。
- 继续固定官方 `dsh-v0.1.2-rc.1` 提交 `a66e4702047846cdaa10c66c9d3df3951f5ea70d`，AgentTeams fork 为 `0.1.15-desktop.6`。

安装包仅作为本地测试资产生成，不进入源码树、不提交、不打 tag、不创建 GitHub Release。
